<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Incluye la configuración base de Spring Boot, que ya define colores y patrones útiles -->
    <include resource="org/springframework/boot/logging/logback/base.xml"/>

    <!-- Define una propiedad para el patrón de la consola -->
    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>

    <!-- Define el Appender para la consola -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- Usa el patrón que definimos arriba -->
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <!-- Aplica la configuración al logger raíz -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>