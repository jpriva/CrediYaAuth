server:
  port: 8081

spring:
  application:
    name: "AUTENTICACION"
  devtools:
    add-properties: false
  profiles:
    include: null
  flyway:
    url: "jdbc:mysql://localhost:3306/auth_db"
    user: "jpriva"
    password: "pass12345"
    locations: "classpath:db/migration"
    enabled: true
  output:
    ansi:
      enabled: ALWAYS

adapters:
  r2dbc:
    host: "localhost"
    port: 3306
    database: "auth_db"
    username: "jpriva"
    password: "pass12345"

management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
  server:
    port: 9090

cors:
  allowed-origins: "http://localhost:4200,http://localhost:8081"

springdoc:
  api-docs:
    info:
      title: "Crediya Auth API Microservice"
      version: "1.0.0"
      description: "This is the API for Crediya Auth Microservice"
  swagger-ui:
    path: /swagger-ui.html


logging:
  level:
    org.springframework.transaction.reactive: DEBUG

security:
  jwt:
    secret: 6wmLjTUj5h4CXtp23BPjlr2IouInKGEBhqVi+LkR1KlIisuE4dKm4vbwWhUKwPsg53CjqX5Uu2L8TshpLXPdWA==
    expiration: 3600
